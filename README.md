### Convert Project

This project are educacional only, doing with rocketseat school.

### Project ideia

The ideia are learn javascript concepts, fundamentus.

### Concepts

-> Regular expression (Regex) -> used to replace input when the user types any character, replacing it with "none". Input only accepts numbers.
-> getElementById -> used to get element in HTML, in this case, "amount"
-> addEventListner -> used to listner every event has changed the input, in this case, "amount"
